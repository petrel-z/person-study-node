你们现在的**核心思路其实是对的**：  
`dfs(u)` 返回一些“还没被下面的人拿走、可以留给祖先的人来拿的链价值”。真正卡时间的是实现方式：`vector` 反复合并、排序、返回拷贝，最坏会很慢。把 `vector + sort` 换成**可合并大根堆**，就稳了。

---

## 关键思路

对每个节点 `u` 维护一个大根堆 `H[u]`。

堆里的每个数表示：

> 从 `u` 的某个子树中，还剩下一条可以被“从祖先走下来的人”拿走的最大链价值。

处理节点 `u` 时：

1. 先把所有儿子的堆合并起来。
    
2. 如果堆为空，说明下面没有可接的链，那么新建一条链，价值是 `a[u]`。
    
3. 如果堆不空，说明从 `u` 往下走可以选择一条最值钱的链继续走，所以把 `a[u]` 加到堆顶那条链上。
    
4. 如果 `cnt[u]` 个人从 `u` 出发，他们一定拿当前堆里最大的 `cnt[u]` 条链，加入答案并弹出。
    
5. 剩下的链继续留给祖先。
    

你们原来的这句：

```cpp
sort(v.begin(), v.end(), cmp);
v[0] += a[u];
```

本质上就是在做：

> 找到最大链，把 `a[u]` 加进去。

所以用大根堆维护就行。

---

## 为什么你们的代码会超时

主要问题是这几个：

```cpp
vector<ll> v1 = dfs(i, u);
for (auto j : v1)
    v.push_back(j);

sort(v.begin(), v.end(), cmp);
```

在某些树形结构下，一个很大的 `vector` 会被一层一层往上复制、合并、排序。  
最坏复杂度可能接近 `O(n^2)`，`n = 3e5` 肯定扛不住。

而且递归 DFS 深度可能到 `3e5`，也有爆栈风险。

---

## 正解复杂度

用左偏树 / 可并堆：

- 每个节点最多产生一个堆节点；
    
- 每次合并是 `O(log n)`；
    
- 总复杂度 `O(n log n)`；
    
- 可以过 `3e5`。
    

---

## AC 写法 C++17

```cpp
#include <bits/stdc++.h>
using namespace std;

using ll = long long;

const int N = 300000 + 5;
const int M = 600000 + 5;

int n, m;

int head[N], to[M], nxt[M], ecnt;

ll a[N];
int cnt[N];

int fa[N], order[N], ordCnt;
int rt[N];

ll ans = 0;

struct HeapNode {
    ll val;
    int l, r;
    int dist;
} hp[N];

int heapCnt = 0;

void addEdge(int u, int v) {
    to[++ecnt] = v;
    nxt[ecnt] = head[u];
    head[u] = ecnt;
}

int newNode(ll x) {
    ++heapCnt;
    hp[heapCnt].val = x;
    hp[heapCnt].l = hp[heapCnt].r = 0;
    hp[heapCnt].dist = 1;
    return heapCnt;
}

int mergeHeap(int x, int y) {
    if (!x || !y) return x ? x : y;

    if (hp[x].val < hp[y].val) swap(x, y);

    hp[x].r = mergeHeap(hp[x].r, y);

    if (hp[hp[x].l].dist < hp[hp[x].r].dist) {
        swap(hp[x].l, hp[x].r);
    }

    hp[x].dist = hp[hp[x].r].dist + 1;
    return x;
}

int popHeap(int x) {
    return mergeHeap(hp[x].l, hp[x].r);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n >> m;

    for (int i = 1; i < n; i++) {
        int u, v;
        cin >> u >> v;
        addEdge(u, v);
        addEdge(v, u);
    }

    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }

    for (int i = 1; i <= m; i++) {
        int p;
        cin >> p;
        cnt[p]++;
    }

    // 非递归建根为 1 的树
    order[++ordCnt] = 1;
    fa[1] = -1;

    for (int i = 1; i <= ordCnt; i++) {
        int u = order[i];

        for (int e = head[u]; e; e = nxt[e]) {
            int v = to[e];
            if (v == fa[u]) continue;

            fa[v] = u;
            order[++ordCnt] = v;
        }
    }

    // 自底向上处理
    for (int idx = ordCnt; idx >= 1; idx--) {
        int u = order[idx];

        int h = 0;

        // 合并所有儿子的堆
        for (int e = head[u]; e; e = nxt[e]) {
            int v = to[e];

            if (fa[v] == u) {
                h = mergeHeap(h, rt[v]);
            }
        }

        // 有链就把 a[u] 加到最大链上；没链就新建一条链
        if (h == 0) {
            h = newNode(a[u]);
        } else {
            hp[h].val += a[u];
        }

        // u 点出发的人直接拿当前最大的若干条链
        while (cnt[u] > 0 && h != 0) {
            cnt[u]--;
            ans += hp[h].val;
            h = popHeap(h);
        }

        rt[u] = h;
    }

    cout << ans << '\n';

    return 0;
}
```

---

## 对你们原代码的最小理解版优化

你们的 `vector<ll> dfs(u, fa)` 其实可以理解成：

```cpp
返回 u 子树里还能被祖先的人拿的若干条链价值
```

所以：

```cpp
vector<ll>
```

要换成：

```cpp
int heapRoot
```

原来：

```cpp
sort(v.begin(), v.end(), cmp);
v[0] += a[u];
```

现在：

```cpp
hp[h].val += a[u];
```

原来：

```cpp
for (int i = 1; i <= k; i++)
    ans += v[i - 1];
```

现在：

```cpp
while (cnt[u] > 0 && h) {
    cnt[u]--;
    ans += hp[h].val;
    h = popHeap(h);
}
```

你们差的不是思路，是数据结构。这个题本质就是：**树上贪心 + 可合并大根堆**。