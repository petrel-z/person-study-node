类组件和普通组件的区别
react设计核心——函数式组件为主，简单，灵活
v18之前没有自己的状态，能引用外部数据的钩子

常用钩子：useState、useEffect、useMemo、useCallBack

useMemo在函数里用，缓存回调函数返回的值，无依赖项，就不会重新计算回调函数里的

状态管理工具：reduc、

通信：props、callBack、context包裹组件（形成共享域）、useRef变量（绑定子组件）、eventBus事件总线（全局类，注册事件，触发事件）

自定义hooks

增删改查模版，CURD思想
定制化，针对hook写文档

Partial工具类型，将后面的接口类型全部变成可选

自定义hook和全局函数的区别
1. 区别
2. 副作用：组件往往需要与外部交互（如网络请求、操作 DOM、订阅事件等），这些操作就是副作用。


约定大于配置
服务端渲染
单页应用：首次加载只有div，其他都需要注入
早期单页面，php模版引擎，返回给浏览器的时候就有数据了
next坏处：慢，基于react又封装了一层，更重量

next（全栈框架）：布局文件，页面文件
适合小型网站，业务不复杂的网站
wersor


竞争压力大
写项目用ai，学会用ai，并理解它的思路
ai不能替代——研究某个方面的源码
个人博客——vercel
eletrol,

markdown转html的插件

