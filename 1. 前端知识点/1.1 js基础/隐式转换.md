转化为：false
- `false`（布尔值本身）
- `0` 和 `-0`（数字 0，包括负 0）
- `''` 或 `""`（空字符串）
- `null`（空值）
- `undefined`（未定义）
- `NaN`（非数字，如 `0/0` 的结果）

转换为：true
- 所有非 0 数字（无论正负，如 `1`、`-5`、`3.14`、`Infinity`）
- 非空字符串（如 `"false"`、`"0"`、`"-100"`）
- 所有对象（包括空对象 `{}`、数组 `[]`、函数 `function(){}`, `null` 除外）
- `true`（布尔值本身）