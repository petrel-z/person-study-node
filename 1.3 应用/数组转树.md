
```ts

// interface Item {
//   id: number;
//   parentId: number | null;
//   [key: string]: any;
// }

// interface TreeNode extends Item {
//   children: TreeNode[];
// }

// const list: Item[] = [
//   { id: 1, parentId: null, name: 'Root' },
//   { id: 2, parentId: 1, name: 'Child 1' },
//   { id: 3, parentId: 1, name: 'Child 2' },
//   { id: 4, parentId: 2, name: 'GrandChild 1' },
// ];

// // 修正后的函数：基于原始数据的parentId构建树形结构
// function arrayToTree<T extends Item>(items: T[]): TreeNode[] {
//   // 1. 初始化节点并存储到Map中（关键修复：先创建对象再赋值）
//   const nodeMap = new Map<number, TreeNode>();
//   const tree: TreeNode[] = [];

//   // 第一步：为每个元素创建带children的节点
//   for (const item of items) {
//     // 关键：先创建完整对象，避免undefined
//     const node: TreeNode = {
//       ...item,
//       children: [],
//     };
//     nodeMap.set(item.id, node);
//   }

//   // 第二步：根据parentId建立父子关系
//   for (const item of items) {
//     const currentNode = nodeMap.get(item.id)!;

//     if (item.parentId === null) {
//       // 根节点直接加入树
//       tree.push(currentNode);
//     }
//     else {
//       // 找到父节点并添加到children
//       const parentNode = nodeMap.get(item.parentId);
//       if (parentNode) {
//         parentNode.children.push(currentNode);
//       }
//       else {
//         // 处理无效parentId的情况
//         tree.push(currentNode);
//       }
//     }
//   }

//   return tree;
// }

// const tree = arrayToTree(list);
// console.log('tree', tree);

// interface Item {
//   id: number;
//   parentId: number | null;
//   [key: string]: any;
// }

// interface TreeNode extends Item {
//   children: TreeNode[];
// }

// const list: Item[] = [
//   { id: 1, parentId: null, name: 'Root' },
//   { id: 2, parentId: 1, name: 'Child 1' },
//   { id: 3, parentId: 1, name: 'Child 2' },
//   { id: 4, parentId: 2, name: 'GrandChild 1' },
// ];

// // 签名函数
// export function arrayToTree<T extends {
//   id: number;
//   parentId: number | null;
// }>(
//   items: T[],
// ): (T & { children: T[] })[] {
//   const arrayTree: (T & { children: T[] })[] = Array.from({ length: items.length });

//   for (let i = 0; i < items.length; i++) {
//     const obj = {
//       ...items[i],
//       children: [],
//     } as T & { children: T[] };
//     // obj.children = arrayTree.slice(2 * (i + 1) - 1, 2 * (i + 1) + 1);
//     console.log('obj', obj);
//     arrayTree.push(obj);
//     // arrayTree[i].children = arrayTree.slice(2 * (i + 1) - 1, 2 * (i + 1) + 1);
//   }
//   for (let i = 0; i < arrayTree.length; i++) {
//     arrayTree[i].children = arrayTree.slice(2 * (i + 1) - 1, 2 * (i + 1) + 1);
//     console.log('arra', arrayTree[i]);
//   }

//   return arrayTree;
// }

// const tree = arrayToTree(list);
// console.log('tree', tree);
```



