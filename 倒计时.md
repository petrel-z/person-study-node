```countdown-to
title: Deadline
startDate: 2025-10-16
startTime: 00:08:00
endDate: 2026-03-01
endTime: 00:08:00
type: circle
color: #ff5722
trailColor: #f5f5f5
updateInRealTime: true
updateIntervalInSeconds: 30
```
```dataviewjs
// 配置部分
const targetDateStr = "2026-03-01"; // 目标日期，修改为你需要的日期
// 获取当前日期
const today = new Date();
const formattedDate = `${today.getFullYear()}-${(today.getMonth() + 1).toString().padStart(2, '0')}-${today.getDate().toString().padStart(2, '0')}`;
// 计算距离目标日期的天数
const targetDate = dv.date(targetDateStr);
const daysToTarget = Math.floor((targetDate - today) / (1000 * 60 * 60 * 24));
// 输出部分
dv.el('div', `目标日期：${targetDateStr}`);
dv.el('div', `距离目标日还剩：<span style="color: red;">${daysToTarget}</span> 天`);
```

