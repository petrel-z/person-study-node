## 1.27
Vue 核心基础
1. computed 和 watch 的核心区别是什么？（重点：缓存）
2. v-model 的原理是什么？（语法糖 + 父子组件双向绑定实现）
3. Vue 中如何自定义过滤器？（Vue2 全局 / 局部注册 + Vue3 替代方案）
4. Vue Router 中如何实现路由跳转？有哪几种方式？
5. Vue 中如何给元素绑定事件？（指令 + 简写 + 传参）
6. 结合 props 单向数据流，说明 v-model 在父子组件中的实现逻辑？

React 核心基础（Hooks 为主）
1. React 中 state 和 props 的主要区别是什么？
2. React 中 useEffect 钩子函数的作用是什么？有哪些常用的使用场景？

HTML 基础 + HTML5 新特性
1. HTML 中 meta 标签有哪些常用属性？viewport 标签的核心取值有哪些？
2. HTML 中原生行内块元素有哪些？行内块元素的核心特征是什么？
3. HTML 中 `<b>` 标签的作用是什么？`<b>` 与 `<strong>` 的核心区别？
4. HTML5 的核心新特性有哪些？（语义化 / 表单 / 多媒体 / API 等）

HTML5 图形绘制
1. Canvas 和 SVG 的核心区别是什么？（面试高频，含适用场景）