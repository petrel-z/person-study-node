# 发大水发
此次会议的收获有：
1. 多看别人写的好的项目
2. 发大水

- 范德萨就
- 范德萨
> fdasf 

项目布局调整：
1. 把设置入口放在左下角，替换掉原本的锁屏图标，换成设置图标✅
2. 点击左下角的用户信息，跳转到设置页面，去除原本在侧边栏的设置导航入口✅       
3. 新建日记中的情绪图标要和快捷方式的情绪图标保持一致，相同情绪的图标要一直，快捷方式的情绪图标悬停时显示情绪的文字解释，都统一成配套的圆脸✅
4. 设置页的布局进行修改，要参考一下chrome 中的设置的布局，呈左右布局，并且具体的设置项要统一✅
5.  值得庆祝页面中(AIInsightCard) ✅：
	1. 三个卡片的样式优化一下，有卡片的样式和效果
	2. 悬停时选中的卡片可以放大，另外两个卡片跟着缩小
	3. 下方评分部分,改成柱形图; 三个卡片的内容要对应,第一个卡片是值得庆祝部分,对应celebration,第二个是需要改进部分,对应warning,第三个是行动建议部分,对应action 

6. 日记模块的布局，参考obsdian ✅
	1. 删除快捷日记的功能，去除JournalDetailPage和JournalEditorPage
	2. 调整生活日记页面的布局，改为左右布局，左侧中，顶部是搜索框，搜索框下方是日记列表（展示情绪图标、日记标题、锁屏图标）；右侧是日记详情，日记详情也是左右布局，日记详情左侧是标题和内容，日记详情右侧是情绪的选择、标签的添加和展示、关联的纬度的添加和展示，是否私密的按钮。
	3. 整体的逻辑是，在左侧的上方有添加日记的图标，点击后调用新增日记接口（传入默认的标题“未命名”，和默认的内容“暂无”），新增后左侧列表会立即显示出来，并选中到这个日记，用户可以修改里面的所有相关的日记操作，日记详情左侧的日记内容右上角有一个图标，一个对钩，用于保存日记的修改
	4. 整体的页面样式、风格和操作逻辑参考软件obsidian
	5. 列表收缩
7. 侧边栏伸缩功能✅

8. 时间轴收缩⭕️
9. 字体要醒目，不能看不清⭕️
10. 整体主题暖色调，温馨⭕️

 优化一下注释,删除多余的注释.必须有：文件头部注释、复杂业务逻辑注释、公共函数文档注释、特殊标记注释、兼容 /hack
  注释、多分支逻辑、常量 / 枚举、关键变量注释；
  多余的：复述代码、无意义、过于细节、过时的注释。

线上bug：
1. 新增偏移事件中，新增后不显示事件列表，控制台报错,偏移新增后没有清空输入框✅（后端ipc没有导入饮食系统的路由）
index-CrSeUpP4.js:42265 Failed to create deviation: TypeError: Cannot read properties of undefined (reading 'id')
    at index-CrSeUpP4.js:42057:18
    at async addDeviation (index-CrSeUpP4.js:42252:22)
2. 饮食系统，保存基准配置后无响应，虽然保存成功，但是没有返回到展示✅
3. api key 配置页面去除连接测试的图标和功能 ✅
4. 时间轴中筛选功能和分页功能❌
5. 设置pin页面，两个pin页面中的卡片需要设置key，否则内容继承✅
6. 用户名长度有限制，限制10个字，mbti必需是四个大写字母✅
7. 导出数据报错，报错信息钉钉✅
8. 设置完pin后，会跳转到日记页面❌
9. 输入框加字数限制✅
10. 历史洞察列表的分页加载、洞察展示内容的位置偏移✅
11. mbti改成固定值✅
12. 饮食系统页面优化，添加图表✅
13. 日记隐私✅
14. 提示框的主题和系统主题应该保持同步，现在出现了主题是浅色，提示框是深色的情况✅


