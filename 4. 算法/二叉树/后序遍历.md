https://leetcode.cn/problems/binary-tree-postorder-traversal/description/?envType=study-plan&id=zhijigangwei-qianduan&plan=zhijigangwei&plan_progress=44apym3

左右中

## 递归法：
先按照中右左的顺序递归，再翻转

```js
/**
 * Definition for a binary tree node.
 * function TreeNode(val, left, right) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.left = (left===undefined ? null : left)
 *     this.right = (right===undefined ? null : right)
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number[]}
 */
var postorderTraversal = function(root) {
    let res = [];
    recursion(root, res);
    return res.reverse();
};

let recursion = (root, res) => {
    if(root === null) {
        return;
    }
    res.push(root.val);
    recursion(root.right, res);
    recursion(root.left, res);
}
```


## 迭代法

思路：
数据结构：栈
什么时候入栈，什么时候弹出
先处理根节点
再处理右节点，再处理左节点
弹出根节点