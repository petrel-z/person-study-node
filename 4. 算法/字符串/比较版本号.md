https://leetcode.cn/problems/compare-version-numbers/description/?envType=study-plan&id=zhijigangwei-qianduan&plan=zhijigangwei&plan_progress=44apym3

字符串拆分、类型转换、对齐比较

第一个不为0的数：它左侧的零不算，右侧的零算

例：
**1.001和1.01**
相当于[1, 1] 和 [1, 1] 挨个比较，两者相等，故为0

例：
**10.1和1.1**
相当于[10, 1] 和 [1, 1] 挨个比较，左边大，故为1

例：
**1.01.1和1.1**
相当于[1, 1, 1] 和 [1, 1]  
对齐：
[1, 1, 1]
[1, 1]
相当于：
[1, 1, 1]
[1, 1, 0]
挨个比较，左边大，故为1

```js
/**
 * @param {string} version1
 * @param {string} version2
 * @return {number}
 */
var compareVersion = function(version1, version2) {
    let vs1 = version1.split('.');
    let vs2 = version2.split('.');
    let i = 0;
    for(; i < vs1.length && i < vs2.length; i++){
        if(Number(vs1[i]) > Number(vs2[i])){
            return 1;
        }else if(Number(vs1[i]) < Number(vs2[i])){
            return -1;
        }else{
            continue;
        }
    }
    while(i < vs1.length){
        if(Number(vs1[i]) != 0){
            return 1;
        }
        i++;
    }
    while(i < vs2.length){
        if(Number(vs2[i]) != 0){
            return -1;
        }
        i++;
    }
    return 0;
};
```