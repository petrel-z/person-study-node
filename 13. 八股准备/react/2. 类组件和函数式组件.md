1. 类组件：包含props、state和生命周期
2. 函数式组件：不包含props、state和生命周期，原本只是一个纯渲染的组件，后来hooks出现后，变成了可交互的组件

类组件的缺点：
1. 代码逻辑往往比较复杂，不易拆分和重构
2. 不同的业务的代码在不同的函数中，可能会导致逻辑重复或者关联逻辑

为什么渐渐不用类组件了，因为React团队的设计理念是，组件的功能是需要什么就组装在一块，而不是像一个容器一样，什么东西都有。