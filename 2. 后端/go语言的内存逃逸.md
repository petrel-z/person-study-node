#### 背景：
工程师需要手动分配内存
#### 定义：
原本分配到栈的变量分配到了堆，延长申明周期（在编译阶段）
#### 五种方式出现内存逃逸:
- 函数返回指针变量
-  println里面被已经逃逸指针引用
- 栈空间不足
- 闭包
- map、切片、chant引用的指针

go通过内存管理机制来解决了内存逃逸问题

#### 影响：
GC频繁，cpu消耗大
#### go的内存逃逸优势：
减少堆内存的分配，c，c++需要手动分配内存，go有内存管理机制
