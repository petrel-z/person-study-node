
1. task1做了项目架子搭建
	1. tscofig、viteconfig、electron两个渲染进程控制的html页面
	2. go cli 预留位置
2. task2做了pet资源放置
3. task3
	1. 事件到状态的映射转换，通过测试来验证对于不合理的参数是否通过测试
	2. 通过state映射对应的动画帧数相关数据，测试配置和映射函数
4. task4
	1. go 的cli：负责获取token、port，解析参数，发送http
	2. 被编译为go二进制文件
5. task5
	1. 应用找到用户目录下的.mypet文件夹里的所有信息，如token、port、log、go hook，主要是用来找到、创建、写入
	2. 用户创建日志文件、写入日志文件
6. task6
	1. 令牌桶：请求限流
	2. 状态队列：放置state，防止状态切换的太快出现闪烁
7. task7
	1. http服务，创建server服务，监听请求，解析body、校验body、请求限流、映射state
8. task8
	1. hook中的command构造，疑问：只构造了command
	2. 在用户电脑中安装cli
9. task9
	1. 将hooks按照固定格式写入agent配置文件中，claude code的
10. task10
	1. 将hooks写入codex
问题：最好按 hooks 数组里的单个 command 过滤，而不是按整个 matcher group 过滤。

11. task11
	1. 健康检查，检查.mypet 里的token、port、cli是否存在
	2. 检查cc、codex的hooks是否添加，codex额外检查config是否开启
12. task12
	1. preload集成，暴漏方法
	2. window窗口创建和设置
	3. ipc通信
13. task13
	1. 渲染pet图片动画
14. task14
	1. 渲染控制页面的内容
15. task15
	1. 程序入口main index
16. task16
	1. 全面测试