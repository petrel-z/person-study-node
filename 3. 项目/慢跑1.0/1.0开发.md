商家入驻走通
# 任务
1. 小程序：金豆明细、金豆详情：9-10
2. 小程序：首页轮播：11-12
3. 小程序：我的小店、店铺设置：13-15
4. 小程序：登录弹窗：16
5. 小程序：推广明细：17-18
6. 小程序：搜索商品：19-21
	1. 搜索后的页面
7. 小程序：商家列表：19-21
	1. 首页商家展示
8. 后台：订单管理：22-24
	1. 展示每一笔订单
9. 小程序：商品管理：25-27
	1. 商品列表展示
	2. 上下架
	3. 新增商品
	4. 删除商品
	5. 编辑商品：先判断是否下架，下架时才能编辑商品
	6. 移动
10. 小程序：分类管理：26-27
	1. 新增分类
	2. 删除分类
11. 小程序：商家详情：27-30
	1. 商家详情信息
	2. 商品列表展示
	3. 商家分类展示

问题：下单时加一个是否让商家立即做的选项，让用户找跑腿的时候，他到店直接取就行，不用等待

商家入驻走通支付流程
商家状态：到期（每年都需要支付）
用户授权模版消息

并发问题：用户在去结算的操作应该和商家编辑商品的操作互斥，也就是商家在编辑商品的时候必须下架商品。
用户和商家谁先抢到锁，谁就先操作，相当于虽然是并发操作，但是由于锁的出现，将操作同步化了。

# 轮播图跳转
参考：[https://blog.csdn.net/weixin_39450691/article/details/144034076]

### 跳转小程序
在manifest.json里配置（是否可以动态配置）
小程序权限配置`navigateToMiniProgramAppIdList`
用户权限授权

开发文档：
https://developers.weixin.qq.com/miniprogram/dev/api/navigate/wx.navigateToMiniProgram.html

### 跳转h5
uniapp组件
https://uniapp.dcloud.net.cn/component/web-view.html#postmessage
使用uniapp里的web-view组件即可实现

### 系统内部跳转
跳转到某个页面


# 我的小店

数据展示：
该店铺近7天或者近30天的
可以一下展示七天的
可以手动看最近四个七天的数据

店铺设置有什么东西？

图表：
```vue
<!-- 订单图表 -->
<template>
  <view>
    <qiun-data-charts type="line" :chart-data="chartData" />
  </view>
</template>

<script setup lang="ts">
const chartData = ref();

const getServerData = () => {
  setTimeout(() => {
    const res = {
      categories: ['2016', '2017', '2018', '2019', '2020', '2021'],
      series: [
        {
          name: '目标值',
          data: [35, 36, 31, 33, 13, 34],
        },
        {
          name: '完成量',
          data: [18, 27, 21, 24, 6, 28],
        },
      ],
    };
    chartData.value = JSON.parse(JSON.stringify(res));
  }, 500);
};

onReady(() => {
  getServerData();
});
</script>

```
- line - 折线图 ✅ (已修改)
- column - 柱状图
- area - 区域图
- bar - 条形图
- pie - 饼图
- ring - 环形图
- radar - 雷达图
- scatter - 散点图
- bubble - 气泡图
- gauge - 仪表盘
- candle - K线图
- mix - 混合图

# 注册弹窗
在注册弹窗添加一个手动输入邀请码的信息（选填）
如果新用户是扫码进来的，邀请码置灰不能填；如果不是扫码进来的，可手动填邀请码



# 购物车
1. 每个商店有一个单独的购物车（只能买同一个店的商品）
2. 不同商店共用一个购物车（跨店购买）
购物车是需要存在数据库里的


# 商品规格

统一规格：价格统一
单规格：只有一个规格（大杯、中杯、小杯，来选）
多规格：多个规格分类
	多个规格会形成不同的组合，每一种组合可以设置价格和库存


# 注册

进到注册页面先检测本地存储中是否有一个邀请码数据
- 如果有
	- 就让注册弹窗中的填写邀请码输入框置灰
	- 在注册时将本地的邀请码进行绑定
- 如果没有
	- 就让填写邀请码的输入框取消置灰
	- 在注册时将用户输入的邀请码进行绑定

wd-input的动态disabled属性设置  , :disabled


# 商家入驻bug修复

两列选择器，会出现，右边列对应为空的情况，这该怎么处理
以及默认值为0的情况怎么设置

经纬度选择

# 后台：订单管理
需求：
超级管理员能看到所有用户订单项，城市运营商看到当前城市中所有用户的订单，团长看到学校的所有用户的订单
	1. 要有订单列表（数据权限）省市维度，时间，商家名称，商品名称，订单号，用户手机号筛选

**筛选：**
超级管理员：省份、城市、学校
城市运营商：学校
团长：
公共：时间（时间范围：开始时间-结束时间）
商家名称、商品名称、订单号、用户手机号 - 模糊查询
**字段：**
用户名、用户id、用户手机号、商家名称、商品名称、订单号、
# ts
type: [Number, null, undefined] as PropType<number | null | undefined>,
这样定义的是props的类型
props的types期望是构造函数或构造函数数组
number是构造函数，但null和undifined不是
ts的写法则需要类型断言

# 如何在小程序内直接让用户手机号打电话


# 微信小程序如何实现拖动排序
### 1. 触摸事件方案
使用微信小程序支持的触摸事件：
- @touchstart - 触摸开始
- @touchmove - 触摸移动
- @touchend - 触摸结束
通过计算触摸位移来判断是否需要交换元素位置。
### 2. 按钮操作方案
在每个分类项右侧添加上下箭头按钮：

- 点击向上箭头：将当前项与上一项交换位置
- 点击向下箭头：将当前项与下一项交换位置
### 修改后的功能特点
- 触摸拖拽：长按并移动来调整顺序
- 按钮操作：点击箭头按钮精确调整位置

```vue
<!-- 拖拽排序区域 -->
    <view v-if="showSortMode" class="p-4">
      <view class="space-y-2">
        <view 
          v-for="(item, i) in categories" 
          :key="item.id"
          :class="[
            'flex items-center justify-between bg-white rounded-lg p-4 border border-gray-200 transition-all duration-200',
            dragIndex === i ? 'opacity-50 scale-95' : ''
          ]"
          @touchstart="handleTouchStart($event, i)"
          @touchmove="handleTouchMove($event, i)"
          @touchend="handleTouchEnd($event, i)"
        >
          <view class="flex items-center gap-3">
            <wd-icon name="drag" size="16px" color="#999" />
            <text class="text-gray-800">{{ item.name }}</text>
            <text class="text-xs text-gray-500">({{ item.count }})</text>
          </view>
          <view class="flex items-center gap-2">
            <wd-icon name="arrow-up" size="14px" color="#666" @click="moveUp(i)" v-if="i > 0" />
            <wd-icon name="arrow-down" size="14px" color="#666" @click="moveDown(i)" v-if="i < categories.length - 1" />
          </view>
        </view>
      </view>
    </view>
    
<script setup lang="ts">

let dragIndex = ref(-1); // 当前拖拽的元素索引值
const showSortMode = ref(false); // 控制排序模式显示
let startY = 0; // 触摸开始的Y坐标
let currentY = 0; // 当前触摸的Y坐标

// 触摸开始
const handleTouchStart = (e: any, index: number) => {
  dragIndex.value = index;
  startY = e.touches[0].clientY;// 当前触摸元素的初始触摸位置
};

// 触摸移动
const handleTouchMove = (e: any, index: number) => {
  currentY = e.touches[0].clientY;
  const deltaY = currentY - startY;
  
  // 根据移动距离判断是否需要交换位置
  if (Math.abs(deltaY) > 50) { // 移动超过50px才触发交换
    const targetIndex = deltaY > 0 ? index + 1 : index - 1;
    if (targetIndex >= 0 && targetIndex < categories.value.length && targetIndex !== dragIndex.value) {
      swapItems(dragIndex.value, targetIndex);
      dragIndex.value = targetIndex;
      startY = currentY; // 重置起始位置
    }
  }
};

// 触摸结束
const handleTouchEnd = (e: any, index: number) => {
  dragIndex.value = -1;
  startY = 0;
  currentY = 0;
};

// 交换数组中两个元素的位置
const swapItems = (fromIndex: number, toIndex: number) => {
  [categories.value[fromIndex], categories.value[toIndex]] = [categories.value[toIndex], categories.value[fromIndex]];
};

// 向上移动
const moveUp = (index: number) => {
  if (index > 0) {
    swapItems(index, index - 1);
  }
};

// 向下移动
const moveDown = (index: number) => {
  if (index < categories.value.length - 1) {
    swapItems(index, index + 1);
  }
};

interface Category {
  id: string;
  name: string;
  count: number;
}

const categories = ref<Category[]>([
  { id: '1', name: '饮料', count: 12 },
  { id: '2', name: '小食', count: 8 },
  { id: '3', name: '主食', count: 15 },
  { id: '4', name: '甜品', count: 6 },
  { id: '5', name: '热饮', count: 9 },
]);
</script>
```

在拖动时想要禁止页面滚动就让父盒子的catchtouchmove="true"
